<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6" >
            <MudIcon Class="mb-n1 mr-3" Color="@DataModel.Color" Icon="@DataModel.Icon" />
            @DataModel.Title
        </MudText>
    </TitleContent>

    <DialogContent>

        @if (DataModel.Fragment != null)
        {
            @DataModel.Fragment
        }
        else
        {
            <MudText>@DataModel.Message</MudText>
        }

    </DialogContent>

    <DialogActions>
        <MudButton Color="Color.Error" OnClick="@Cancel" Variant="Variant.Outlined" >@AppLang.Cancel</MudButton>
        <MudButton Color="Color.Success" OnClick="@Submit" Variant="Variant.Filled" >@AppLang.Submit</MudButton>
    </DialogActions>
</MudDialog>